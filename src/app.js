const express = require('express');
const app = express();
const pool = require('./config/db');

const getByIDTransacao = require()
const getTransacao = require()
const postTransacao = require()

app.use(express.json());
module.exports = app